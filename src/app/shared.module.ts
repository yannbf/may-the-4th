import { NgModule } from '@angular/core';
export const ELEMENTS = [
]

@NgModule({
    declarations: [
        ELEMENTS
    ],
    imports: [
    ],
    exports: [
        ELEMENTS
    ]
})

export class SharedModule { }
